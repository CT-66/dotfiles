#!/bin/sh
xmodmap "$HOME/Dotfiles/X11/Xmodmap"
xrdb -merge "$HOME/Dotfiles/X11/Xresources"

if [ -d /etc/X11/xinit/xinitrc.d ] ; then
 for f in /etc/X11/xinit/xinitrc.d/?*.sh ; do
  [ -x "$f" ] && . "$f"
 done
 unset f
fi

exec openbox-session
exec $HOME/Dotfiles/startup.sh &
